# 1.0.0 验证记录

验证日期：2026-09-18。构建环境：Windows x64，Python 3.13.5，PySide6 6.8.3，PyInstaller 6.22.3。

- Python 42 项测试通过，包括配置迁移、路线模型、图搜索、闭环、采样、模拟接口、线程生命周期和安装后配置目录。
- Node 地图运行测试通过，包括路径缓存、绘制合并、覆盖层复用、撤销与取消任务。
- 正式路网 370 节点、433 边；原有查询来源全部保留。相对于补路前净增 21 节点、30 边。
- 固定旧图的 36 次距离规划结果与原实现一致，耗时中位数 2.175 秒降至 0.0124 秒；不代表整机性能。
- 源码及安装资源不包含本地配置、账户 Cookie、用户 ID、腾讯 Key、采样缓存或开发日志。
- 图标包含 16、24、32、48、64、128、256 像素 ICO 图层。
- PyInstaller 构建成功；Inno Setup 6.7.3 安装包编译成功，大小 126384150 字节。
- 独立目录静默安装退出码为 0；2847 个安装文件与构建输出逐一 SHA-256 对比，全数一致。
- 安装后程序从不同工作目录启动，在独立用户数据目录与 offscreen 环境中持续运行 10 秒，配置目录创建成功。
- 测试卸载退出码为 0，应用文件已移除，用户配置目录保留。

隔离启动仍记录 Qt WebEngine 的 GLES context 警告，程序在观察期间未退出；该检查不能替代可见窗口下的在线地图交互或长期稳定性验证。安装包未作代码签名。

腾讯在线底图需要用户自己的 Key；真实学校接口上传和整机长期稳定性未作为本次构建验证项目。
